const image = {
    id: 'IMAGE_ID',
    userId: 'USER_ID',
    parentId: 'PARENT_ID',
    reportId: 'REPORT_ID', //if any
    type: 'FROM CONSTANTS',
    date: 'epoch',
    title: 'IMAGE TITLE',
    description: 'IMAGE DESCRIPTION',
    // ? find which is better
    // ! search for the photo library used with facebook and try to unerstand everything about it
    // ! how to upload an image to the server and get it's url
    // add image as binary to DB
    // copy image to server and get it's URL
    image: 'BINARY OR URL'
};
